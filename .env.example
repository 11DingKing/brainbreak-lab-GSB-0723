# focuslab 环境变量示例。复制为 .env 并按需修改。

# HTTP 监听地址（默认 :8080）
FOCUS_ADDR=:8080

# PostgreSQL 连接串。设置后使用 PG 后端（启动时自动迁移）；
# 不设置则回退到内存后端。
# FOCUS_PG_DSN=postgres://user:pass@localhost:5432/focuslab?sslmode=disable
